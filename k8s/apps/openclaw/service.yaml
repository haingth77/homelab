apiVersion: v1
kind: Service
metadata:
  name: openclaw
  namespace: openclaw
  labels:
    app.kubernetes.io/name: openclaw
    app.kubernetes.io/instance: openclaw
spec:
  type: NodePort
  ports:
    - port: 18789
      targetPort: http
      nodePort: 30789
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: openclaw
    app.kubernetes.io/instance: openclaw
